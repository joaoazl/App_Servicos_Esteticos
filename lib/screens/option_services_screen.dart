import 'package:flutter/material.dart';

class OptionServicesScreen extends StatelessWidget {
  const OptionServicesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Ola'),
    );
  }
}